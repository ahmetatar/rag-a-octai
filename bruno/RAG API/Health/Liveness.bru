meta {
  name: Liveness
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

docs {
  Liveness probe. Never touches ChromaDB or Ollama, so it stays fast even if a dependency
  is down. Returns { status: 'ok', uptime }.
}
